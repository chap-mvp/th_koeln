#include <stdio.h>
#include <time.h>

int getCurrentDate () {
    // current date in form: dd.mm.yyyy
}

int getNumWhitespaces () {
    // prints the number of spaces in a string
} 

int quadraticFunc (int p, int q, int x) {
    // enter the values for p and q and calculate y for an x value (f(x) = x^2 + px + q)
}

int getZeroPointsQuadraticFunc () {
    // all zero points of a quadratic equation
}

int swap () {
    // swap the values of x and y
}

float polynom () {
    
}

int sequence () {
    // calculate n values of an arithmetic series
}

int main () {
    
    printf("You can do the following tasks, for a task of your choice, enter its number\n");

    int p;
    int q;
    int x;

    prinf("To get the value of a function f(x) at x, give p, q and x: f(x) = x^2 + px + q - ");
    scanf("%d, %d, %d", &p, &q, &x);
    quadraticFunc(p, q, x);
    

}